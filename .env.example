# Leave empty for local `npm run dev` — Vite proxies /v1 → http://localhost:4000
VITE_API_URL=

# Vercel: set this as an Environment Variable (Production + Preview), then redeploy.
# No trailing slash. Example:
# VITE_API_URL=https://lattice-api.onrender.com
